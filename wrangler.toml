name = "claude-ai-resume"
compatibility_date = "2024-01-01"

# This project runs under Cloudflare Workers Builds, whose CI-managed API
# token only has Workers permissions (not Cloudflare Pages). Deploy static
# assets directly via `wrangler deploy`, not `wrangler pages deploy`.
[assets]
directory = "."
