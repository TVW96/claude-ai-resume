name = "claude-ai-resume"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration - required since the deploy command
# ("npx wrangler pages deploy") is run without a directory argument
pages_build_output_dir = "."
